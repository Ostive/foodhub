// Simple auth state management for testing purposes
// In a real app, this would be replaced with a proper auth context/provider

// Using a reactive object to allow updating the value across components
export const isLoggedIn = {
  value: false
};
